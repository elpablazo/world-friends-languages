import { CollectionConfig } from "payload/types";

const Clases: CollectionConfig = {
  slug: "clases",
  fields: [
    {
      name: "nombre",
      type: "text",
    },
  ],
};

export default Clases;
